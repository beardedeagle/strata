//! Workspace-owned source-to-runtime gates.
//!
//! The executable assertions live in integration tests so the gate crate
//! stays outside Strata-owned and Mantle-owned implementation crates.

#![forbid(unsafe_code)]
