[package]
name = "source-to-runtime-gates"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"

[dev-dependencies]
mantle-artifact = { path = "../mantle-artifact" }

[lib]
name = "source_to_runtime_gates"
path = "src/lib.rs"
