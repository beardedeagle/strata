[package]
name = "mantle-artifact"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "mantle_artifact"
path = "src/lib.rs"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.2", default-features = false, features = ["fs"] }
